## my title

sidjfaiño oifoiasdfhoisfda ug

```js
/** @module Math */


/**
 * @description Add to numbers
 * @param {Number} x
 * @param {Number} y
 * @returns {Number} sum of x and y
 */
const add = (x, y) => x + y


/**
 * @description substract two humber
 * @param {Number} x
 * @param {Number} y
 * @returns {Number}
 */
const substract = (x, y) => x - y


module.exports = {
    add,
    substract
}

```

